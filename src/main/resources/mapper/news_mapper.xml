<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.jobx_batch.dao.NewsDao">

    <insert id="insertNews" parameterType="com.example.jobx_batch.dto.NewsDto">
        INSERT INTO news (
            author, title, description, url,
            url_to_image, published_at, content, source_id
        ) VALUES (
            #{author}, #{title}, #{description}, #{url},
            #{urlToImage}, #{publishedAt}, #{content}, #{sourceId}
        )
    </insert>


    <insert id="insertNewsSource" parameterType="com.example.jobx_batch.dto.SourceDto">
        INSERT IGNORE  INTO source (
            name
        ) VALUES (
            #{name}
        )
    </insert>

    <select id="selectNewsSource" parameterType="com.example.jobx_batch.dto.SourceDto">
        SELECT
            s.id
        FROM
            source s
        WHERE
            s.name = #{name}
    </select>

</mapper>
